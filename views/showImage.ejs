<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show your Image</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <a href="/" class="btn btn-primary row">Back to Home</a> <br>
    <a href="/file" class="btn btn-primary row">Add new files</a>
    <div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1">
        <% for(let image of findedImage){ %>

                <div class="card listing-card" style="width: 20.7rem;">
                    <img src="<%= image.link %>" class="card-img-top" alt="listings-img" style="height: 20rem;">
                    <form action="/image-delete/<%= image._id %>?method=DELETE" method="post">
                        <button class="btn btn-danger">Delete</button>
                    </form>
                </div>
        <% } %>
    </div>
</body>
</html>